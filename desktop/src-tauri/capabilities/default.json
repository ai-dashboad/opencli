{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions this window needs. Tauri v2 grants none by default, and an ungranted capability fails silently — which is why dragging the window by its title strip did nothing at all.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
