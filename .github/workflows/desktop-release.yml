# Desktop app bundles for a release.
#
# Called by `rust-release.yml` so that everything a release contains — the CLI
# binaries and the desktop app — lands in one GitHub Release under one tag.
# Two workflows publishing to the same tag would race each other.
#
# Each platform builds natively. Cross-compiling would be faster to configure
# and wrong in a way that is hard to see: the app does not run the agent
# itself, it runs the `opencli` binary staged next to it, and that binary is
# built for whatever `rustc` calls the host.

name: desktop-release

on:
  workflow_call:
  # Also runnable by hand, to check the bundles build without cutting a
  # release.
  workflow_dispatch:

jobs:
  build:
    name: ${{ matrix.platform }}
    runs-on: ${{ matrix.runner }}
    # Generous, because the expensive part is not the build. A cold Intel macOS
    # runner compiles the agent and the shell from nothing and then spends
    # minutes writing the cache back; the job that was killed at the ninety
    # minute mark had already built, staged and uploaded everything, and died
    # saving a cache.
    timeout-minutes: 150

    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-14
            platform: darwin-aarch64
            target: aarch64-apple-darwin
            bundles: app,dmg
          # `macos-13` was retired; Intel builds run on `macos-15-intel`.
          - runner: macos-15-intel
            platform: darwin-x86_64
            target: x86_64-apple-darwin
            bundles: app,dmg
          - runner: ubuntu-22.04
            platform: linux-x86_64
            target: x86_64-unknown-linux-gnu
            bundles: appimage,deb
          - runner: windows-latest
            platform: windows-x86_64
            target: x86_64-pc-windows-msvc
            bundles: msi,nsis

    steps:
      - uses: actions/checkout@v7

      - uses: dtolnay/rust-toolchain@1.100

      # The workspace patches `tokio-tungstenite` to a fork whose own
      # dependency is written as `ssh://git@github.com/...`. A runner has no
      # SSH key, so the fetch fails with "failed to load source for dependency"
      # after several minutes of compiling — on a developer's machine it works,
      # because their key is right there.
      - name: Fetch git dependencies over HTTPS
        shell: bash
        run: git config --global url."https://github.com/".insteadOf "ssh://git@github.com/"

      # The sidecar is the expensive half and is cached; the desktop crate's
      # own target directory is not. Caching it linked a stale Windows
      # resource file alongside the freshly generated one and the build died
      # on "CVT1100: duplicate resource, type:VERSION" — a failure with no
      # connection to anything in the source.
      - uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            opencli-rs/target/
          key: desktop-${{ matrix.platform }}-${{ hashFiles('**/Cargo.lock') }}

      - uses: pnpm/action-setup@v6
        with:
          run_install: false

      - uses: actions/setup-node@v7
        with:
          node-version: 22

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile

      # WebKitGTK is the web view on Linux; without it the crate does not build.
      - if: ${{ startsWith(matrix.runner, 'ubuntu') }}
        name: Install Linux build dependencies
        run: |
          sudo apt-get update -y
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y \
            libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf \
            libssl-dev build-essential file

      - uses: cargo-bins/cargo-binstall@main
      # Fetched for this runner's own architecture, explicitly.
      #
      # Tauri reads the sidecar's expected triple off the architecture of this
      # binary rather than off rustc, so an x86_64 build of it on the arm64
      # macOS runner — which is what was picked one morning, having worked the
      # day before — sent the bundler looking for
      # `opencli-x86_64-apple-darwin` while the staging script had just written
      # `opencli-aarch64-apple-darwin`.
      - name: Install the Tauri CLI
        run: cargo binstall --no-confirm --targets ${{ matrix.target }} tauri-cli@^2

      # `beforeBuildCommand` runs `stage-sidecar.sh` and builds the frontend,
      # so the app cannot be bundled around a stale agent.
      #
      # Built for the host, without `--target`.
      #
      # Naming the target here also worked, and cost an hour: cargo then
      # separates host units from target units and compiles the build
      # dependencies twice, which on the Intel macOS runner took the job from
      # comfortably inside its ninety minutes to being killed at ninety minutes
      # and one — with the app built and uploaded, and only the cache save left
      # to do. The triple is settled by installing a native CLI above, which
      # costs nothing.
      #
      # `OPENCLI_TARGET` still says which triple this is meant to be, so the
      # staging script names the sidecar deliberately rather than by inference.
      - name: Build the app
        working-directory: desktop
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          OPENCLI_TARGET: ${{ matrix.target }}
        run: cargo tauri build --bundles ${{ matrix.bundles }}

      # What the build script actually asked the linker for.
      #
      # The Windows bundle dies in CVTRES with "duplicate resource,
      # type:VERSION", and the link command shows the same generated
      # `resource.lib` passed twice — which nothing in `embed-resource`, which
      # emits it once, accounts for. This prints the build script's own output
      # so the second one can be attributed rather than guessed at; it costs a
      # few lines on a green run and is the whole diagnosis on a red one.
      - if: ${{ always() && startsWith(matrix.runner, 'windows') }}
        name: Show what the build script emitted
        shell: bash
        run: |
          set -euo pipefail
          find desktop/src-tauri/target -path '*opencli-desktop-*/output' -print \
            -exec grep -n 'link-arg\|link-lib\|link-search' {} \; 2>/dev/null || true

      # Two kinds of file come out of here, and they are named differently on
      # purpose.
      #
      # The updater's artifacts keep the names Tauri gave them, because they are
      # paired with a `.sig` and the manifest finds them by that pairing.
      #
      # The files a person downloads are copied to names with no version in
      # them, so that `releases/latest/download/OpenCLI-macos-aarch64.dmg` is a
      # link that never has to be edited — the download page, the README and
      # every answer anyone ever gives in an issue can use it.
      - name: Stage bundles
        shell: bash
        run: |
          set -euo pipefail
          dest="dist-desktop/${{ matrix.platform }}"
          mkdir -p "$dest"
          bundle="desktop/src-tauri/target/release/bundle"
          arch="${{ matrix.platform }}"
          arch="${arch#*-}"

          # `find` rather than `shopt -s globstar`: the Intel macOS runner has
          # bash 3.2, where globstar does not exist and the option itself is an
          # error — after twenty minutes of compiling.
          find_bundles() {
            find "$bundle" -type f -name "$1" 2>/dev/null || true
          }

          # Updater artifacts, renamed to carry the platform.
          #
          # Tauri names the macOS one `OpenCLI.app.tar.gz` with no architecture
          # in it, so both Mac jobs would upload the same file name into the
          # same release and one would silently replace the other — leaving
          # half the users updating into a build for the wrong processor. The
          # extension is preserved exactly, because the updater decides how to
          # install from it.
          find_bundles "*.sig" | while read -r signature; do
            artifact="${signature%.sig}"
            name="$(basename "$artifact")"
            case "$name" in
              *.app.tar.gz) renamed="OpenCLI-${{ matrix.platform }}.app.tar.gz" ;;
              *.AppImage)   renamed="OpenCLI-${{ matrix.platform }}.AppImage" ;;
              *-setup.exe)  renamed="OpenCLI-${{ matrix.platform }}-setup.exe" ;;
              *.msi)        renamed="OpenCLI-${{ matrix.platform }}.msi" ;;
              *)            renamed="$name" ;;
            esac
            cp "$artifact" "$dest/$renamed"
            cp "$signature" "$dest/${renamed}.sig"
          done

          # Human downloads, under names with no version in them so the links
          # on the site and in the README never have to be edited. On Linux and
          # Windows this is the same file the updater uses, already staged
          # above; copying it again would only add a name nothing points at.
          find_bundles "*.dmg" | while read -r file; do
            cp "$file" "$dest/OpenCLI-macos-${arch}.dmg"
          done
          find_bundles "*.deb" | while read -r file; do
            cp "$file" "$dest/OpenCLI-linux-${arch}.deb"
          done

          ls -l "$dest"
          if [ -z "$(ls -A "$dest")" ]; then
            echo "no bundles were produced for ${{ matrix.platform }}" >&2
            exit 1
          fi

      - uses: actions/upload-artifact@v7
        with:
          name: desktop-${{ matrix.platform }}
          path: dist-desktop/${{ matrix.platform }}/*
