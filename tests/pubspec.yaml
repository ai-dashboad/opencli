name: opencli_e2e_tests
description: End-to-end tests for OpenCLI system
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  web_socket_channel: ^2.4.0
  crypto: ^3.0.3
  http: ^1.1.0

dev_dependencies:
  test: ^1.24.0
  lints: ^2.1.1
