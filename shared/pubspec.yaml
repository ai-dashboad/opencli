name: opencli_shared
description: Shared code and protocols for OpenCLI ecosystem
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # No external dependencies - pure Dart

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
