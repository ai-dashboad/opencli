---
slug: /
title: OpenCLI
sidebar_position: 1
---

# OpenCLI

Every frontier model has an agent of its own. Claude has Claude Code, OpenAI
has Codex, Gemini has its CLI.

OpenCLI is a terminal agent and a desktop app that point at whatever
OpenAI-compatible endpoint you like: a model on the machine in front of you,
your own gateway, or a hosted provider when you want one. It ships with no
models, no accounts and no keys.

## What it does

It reads files, runs commands and edits things — which is as true of a folder
of invoices as it is of a repository. The desktop app is organised the way an
office is:

- a **department** is a directory with standing instructions
- a **bot** is a chat inside one, with a job
- a **duty** is a job that comes round on its own and asks you when it is stuck

## Where to start

| If you | Read |
| --- | --- |
| have not installed it | [Install](/getting-started/install) |
| have installed it | [Point it at a model](/getting-started/point-at-a-model) |
| have it answering | [First conversation](/getting-started/first-conversation) |
| want it doing real work | [Departments and bots](/doing-work/departments-and-bots) |

## Before you spend an evening on it

Two things worth knowing up front, both of them said again in
[Limits](/reference/limits):

- **A small local model is not as good at this as a frontier one.** The
  interface is the same; the reasoning is not.
- **The desktop builds are not signed** with an Apple or Microsoft
  certificate, so your operating system will warn you the first time.
