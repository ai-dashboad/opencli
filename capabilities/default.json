{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions the window needs. Tauri v2 grants none by default, so an ungranted capability fails silently — dragging the window by its title strip does nothing at all.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
