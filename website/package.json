{
  "name": "@ai-dashboad/opencli-website",
  "version": "0.1.0",
  "private": true,
  "description": "opencli.ai — what OpenCLI is, and where to download it.",
  "type": "module",
  "scripts": {
    "dev": "pnpm run stage && vite",
    "stage": "cp ../scripts/install.sh public/install.sh",
    "build": "pnpm run stage && tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
